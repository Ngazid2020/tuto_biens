{% extends 'base.html.twig' %}

{% block body %}
<div class="jumbotron text-center">
    <H1>Bienvenue sur ma page</H1>
    <p>
        jkid jdui oleid jdeu die dh e dieuf e çfçffihrrr çfpsds fvjbcx,vcx vxcv uçre_ n'( ih fsdçfub) ifosdfbfid
        dsfoudsbfds f dfuydf dsyf_es_e_ çezpefzpjfe ç_ à ç_f   fjzepgqp _'_ç) z_'z_(èz'(à( à)))
        erzrio^ze ^ orie rue_rçzhemqheré&mo oj  uyfez _  )"_'ç
    </p>   
    
</div>
<div class="container">
        <h2 class>Les derniers biens</h2>
        <div class="row flex">
            {% for property in properties %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                            </h5>
                            <p class="card-text">{{ property.city }} ({{ property.postalCode }})</p>
                            <div class="text-primary" style="font-weight: bold; font-size: 2rem">{{ property.formattedPrice }}</div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}